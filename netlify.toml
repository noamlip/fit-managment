# Monorepo: install at repo root, build the Nx web app, publish Vite output.
# Connect the repository in Netlify and enable Deploy Previews on pull requests.

[build]
  publish = "apps/web/dist"
  command = "npm ci && npx nx run web:build"

[build.environment]
  NODE_VERSION = "22"
